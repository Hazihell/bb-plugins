# Plan

1. Add machine-model-preferences package with versioned storage helpers, tests, icon, and settings section.
2. Register plugin in `.bb/plugins.json` and root catalog; document PR #1964 and extension limitation.
3. Run focused checks and root `npm run check`; record evidence.
4. Prepare marketplace entry/release details, stopping before remote mutation for approval.
