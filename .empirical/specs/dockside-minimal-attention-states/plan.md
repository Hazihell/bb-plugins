# Plan: Dockside Minimal Attention States

1. Add pure root/child secondary-state selection and semantic tone helpers with
   focused precedence/tone tests.
2. Compress PR, Done, and family activity components to inline state-only
   rendering while preserving PR accessibility and output-backed Done gating.
3. Move root/child metadata into existing branch lines and strengthen existing
   status words; retain connector tint, hooks, anchors, actions, and hierarchy.
4. Run Dockside tests/typecheck/lint, exercise the live watcher, capture wide
   and compact minimal-state screenshots, and prove no visible output prose.
5. Run root checks, fresh-context outcome QA, isolated committed-diff review,
   integrate the capability delta, and commit the final minimal layer.
