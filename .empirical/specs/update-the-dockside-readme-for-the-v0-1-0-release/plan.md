# Plan

1. Copy the verified light, dark, and expanded-subagent PNGs from thread storage
   into `plugins/dockside/docs/media/` with stable v0.1.0 filenames.
2. Update the README introduction with a release badge, concise shipped-feature
   summary, side-by-side light/dark showcase, and full-width subagent detail.
3. Replace obsolete unpublished/npm-only installation guidance with the public
   immutable Git range command; state marketplace PR #162 as validated and
   awaiting maintainer merge.
4. Refine Cards, Inline agents, and Configuration copy to match the shipped six
   states, parent-only PR metadata, provider marks, semantic color settings,
   and project/family sorting while preserving every existing guide section.
5. Validate image signatures, dimensions, checksums, Markdown references,
   release/tag/install facts, required retained headings, absence of generated
   output, and `git diff --check`.
