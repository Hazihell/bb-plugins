# Plan

1. Add pure family-status and project-order modules with exhaustive state,
   threshold, parsing, permutation, pinned-partition, and conflict tests.
2. Split palette `idle` into `inactive` and `stale`, add server settings/CSS
   variables, and replace Settings dots with an accurate six-state preview.
3. Refactor the root card into a fixed two-row grid with one leading semantic
   icon, explicit badge, elapsed time, root-only PR, combined child/provider
   disclosure, focus tooltips, receded quiet copy, and unchanged child rows.
4. Apply stored order before filters in ThreadInbox; wire ProjectGroup/ThreadCard
   explicit drag handles, Alt+Arrow moves, disabled reasons, and live-region
   announcements without touching navigation/split/selection overlays.
5. Extend layout/settings/interaction tests and update Dockside documentation.
6. Run focused tests/typecheck/build, repair only in-scope failures, and record
   Empirical automated receipts.
7. Install/reload Dockside, exercise all six states at normal/narrow widths plus
   reorder conflicts, capture screenshot/live-result evidence, and record fresh
   context.
8. Complete independent review and capability integration, commit all results,
   push the current branch, update PR #26, and report evidence and blockers.
