# Plan

1. Add persistent Dockside setting descriptors and a pure tested resolver for
   palettes, custom hex fallback, CSS variables, density, expansion, and
   metadata visibility.
2. Register an effective-palette settings preview; consume live settings in
   ThreadInbox and thread preferences through ProjectGroup/ThreadCard/children.
3. Apply semantic variables to working, waiting, unread, error, idle, family
   activity/connectors, and seven PR roles without changing non-color meaning.
4. Switch the root overlay from navigation to ordinary/Shift selection only in
   selection mode, with protected rows consuming the click safely.
5. Add focused tests/system assertions, run Dockside checks, exercise the real
   settings and sidebar surfaces, capture screenshots, then verify and review.
