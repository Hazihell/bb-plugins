import test from 'node:test'; import assert from 'node:assert/strict'; import { clearPreferences, listPreferences, readPreference, writePreference } from '../lib/preferences.ts';
function installStorage(){const map=new Map<string,string>(); (globalThis as any).window={localStorage:{get length(){return map.size},key:(i:number)=>[...map.keys()][i]??null,getItem:(k:string)=>map.get(k)??null,setItem:(k:string,v:string)=>map.set(k,v),removeItem:(k:string)=>map.delete(k)}};}
test('isolates host and provider selections',()=>{installStorage(); writePreference({hostId:'local',providerId:'codex',model:'o3',reasoningLevel:'high'}); writePreference({hostId:'vps',providerId:'cursor',model:'sonnet',reasoningLevel:'medium'}); assert.equal(readPreference('local','codex')?.model,'o3'); assert.equal(readPreference('vps','cursor')?.model,'sonnet'); assert.equal(listPreferences().length,2);});
test('ignores malformed values and clears only plugin keys',()=>{installStorage(); (globalThis as any).window.localStorage.setItem('bb.machine-model-preferences.v1:bad:provider','not-json'); (globalThis as any).window.localStorage.setItem('other','keep'); assert.equal(readPreference('bad','provider'),null); clearPreferences(); assert.equal((globalThis as any).window.localStorage.getItem('other'),'keep');});
