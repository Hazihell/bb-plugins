# Verdict: CHANGES_REQUESTED

- FAIL AC-1: Foreground status and z-10 metadata controls sit above the overlay, so clicking those eligible-row areas does not reach row selection.
- FAIL AC-2: Protected overlay no-ops, but pointer-enabled PR UrlLink above it can still navigate in selection mode.
- PASS AC-3: Declarative persistent palettes and Default reset are implemented.
- PASS AC-4: All thread/PR custom colors use normalized strict six-digit hex validation and safe per-role fallback.
- PASS AC-5: Live owner-scoped variables recolor status/family/PR presentation while non-color semantics remain.
- PASS AC-6: Density, expansion, provider, PR, and time settings are wired with current defaults.
- PASS AC-7: One effective-palette settings preview explains reset/custom behavior.
- FAIL AC-8: The focusable link overlay mutates checkbox state without checkbox semantics and pointer-active siblings bypass selection.

## Security / correctness

Color input is injection-safe, but selection-mode correctness is incomplete because foreground siblings bypass the overlay and a protected PR link can navigate.

## Design / maintainability

Settings architecture is clean; selection responsibility is split across a link overlay, native checkbox, and independently interactive foreground controls, creating inconsistent hit testing and accessibility.
