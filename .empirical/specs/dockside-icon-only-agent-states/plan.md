# Plan

1. Restore semantic PR icons and update pure precedence tests so children never
   select Done.
2. Render accessible icon-only root activity/completion and PR icon+number;
   remove child completion requests and child status text.
3. Make root trailing status age-only, run focused checks, and verify live
   wide/compact rows with three quiet children.
4. Run root checks, fresh QA, committed review, integration, and commit all
   final evidence.
