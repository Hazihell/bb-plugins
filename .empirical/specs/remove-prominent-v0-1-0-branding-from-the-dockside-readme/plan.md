# Plan

1. Remove the release badge and promotional version wording from the README
   hero, introduction, showcase heading, image alt text, and Install lead.
2. Retain `^0.1.0`, `dockside/`, and the immutable tag link only in Install.
3. Confirm the Discord/download announcement is absent, screenshots resolve,
   operational sections remain, and `git diff --check` passes.
