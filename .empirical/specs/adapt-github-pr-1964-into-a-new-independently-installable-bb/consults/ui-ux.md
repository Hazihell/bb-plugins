# UI/UX consult

Verdict: advisory

The settings section should use a compact table with host, provider, model, and
reasoning columns, plus a clearly labeled destructive “Clear saved selections”
button. Explain that these are plugin-managed values and link PR #1964. Keep
the empty state explicit and avoid implying that the built-in picker is already
overridden.
